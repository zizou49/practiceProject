version: 2.1

jobs:
  build:
    docker:
      - image: node:10.19.0-alpine3.11
    steps:
      - checkout
      - run:
          name: Build Docker Image
          command: |
            TAG=2.6.20
            docker build -t myapp-image:$TAG .

workflows:
  build_docker_image:
    jobs:
      - build
