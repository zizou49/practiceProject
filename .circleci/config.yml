version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:14.17-browsers
    steps:
      - checkout
      - run:
          name: Docker version
          command: whoami
      
workflows:
  build_docker_image:
    jobs:
      - build
